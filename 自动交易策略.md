# Longo 跨所套利自动交易策略文档

**版本**: v1.0  
**日期**: 2026-05-20  
**状态**: 待专家评审  
**目标读者**: 量化交易专家、风控专家  

---

## 1. 背景与目标

### 1.1 我们在做什么？

Longo 是一个**跨交易所套利自动化交易平台**，专注于永续合约市场的统计套利。

**核心逻辑**：
- 同一币种（如 BTC/USDT）在不同交易所（Binance、OKX、Bybit）之间存在短暂价差
- 在价格低的交易所做多，在价格高的交易所做空，建立对冲头寸
- 等待价差回归后同时平仓，赚取价差收益
- 通过资金费率差获取额外持仓收益

**关键特征**：
- **非搬砖**：不涉及提币，纯合约对冲
- **市场中性**：理想情况下 Delta ≈ 0，不依赖方向性行情
- **高频监控 + 低频交易**：毫秒级价差监控，但交易频率受信号质量严格控制
- **风控优先**：宁可错过机会，不可承受不可控风险

### 1.2 核心挑战

| 挑战 | 说明 |
|------|------|
| **单边成交风险 (Leg Risk)** | 一条腿成交，另一条腿未成交，形成裸敞口 |
| **滑点侵蚀利润** | 订单簿深度不足导致实际成交价偏离预期 |
| **资金费率波动** | 持仓期间费率变化可能吞噬利润 |
| **交易所异常** | API 延迟、宕机、拔网线 |
| **假突破信号** | 价差瞬间扩大后迅速恢复，触发后反而亏损 |

---

## 2. 自动下单策略

### 2.1 总体流程图

```
[行情采集] → [价差计算] → [打分模型] → [信号确认] → [风控检查] → [下单执行]
    ↑                                                              ↓
    └────────────────── 持续监控 ←────────────────────────── [状态同步]
```

### 2.2 下单触发条件（全部满足才触发）

| 条件 | 默认值 | 说明 |
|------|--------|------|
| **自动交易开关** | OFF | 总开关，关闭时仅推送信号 |
| **评分 ≥ 阈值** | 80 分 | 打分模型输出 |
| **价差率 ≥ 阈值** | 0.1% | 扣除成本后仍有利润空间 |
| **信号持续时长** | 10 秒 | 信号需稳定维持此时长 |
| **并发持仓 < 上限** | 3 | 避免过度分散资金 |
| **单所敞口 < 上限** | $10,000 | 单交易所累计持仓限制 |
| **账户余额充足** | - | 可用保证金 ≥ 预估占用 × 1.2 |

### 2.3 信号持续时长机制（防假突破）

**问题**：价差可能因单笔大单瞬间扩大，随后迅速恢复。若立即下单，可能在价差已回归时成交，导致亏损。

**解决方案**：
1. 首次检测到满足条件的信号时，记录 `signal_start_time`
2. 持续监控，若信号中断（评分跌破阈值或价差率不足），重置计时
3. 仅当信号**连续**维持 `signal_duration`（默认 10 秒）后才触发下单
4. 若信号持续超过 `signal_duration × 2`（默认 20 秒）仍未执行，视为过期信号，丢弃

**临界场景考虑**：
- 场景 A：信号维持 9 秒后消失 → 不触发 ✅
- 场景 B：信号维持 11 秒后消失 → 触发，但下单时价差可能已变化 → 需重新校验当前价差
- 场景 C：信号在 10 秒内波动（8 秒达标 → 2 秒不达标 → 继续达标）→ 重置计时，需重新连续 10 秒

### 2.4 打分模型（Scoring Model）

#### 2.4.1 公式

```
Score = Σ(Factor_i × Weight_i) × 100
```

#### 2.4.2 因子详解

| 因子 | 权重 | 计算方式 | 得分范围 | 说明 |
|------|------|----------|----------|------|
| **价差收益** | 35% | `min((spread% - min_spread%) / 0.005, 1.0)` | 0-100 | 扣除最小阈值后的利润空间，0.5% 价差率得满分 |
| **深度/滑点** | 20% | `min(min_depth_usdt / target_position, 1.0)` | 0-100 | 两侧订单簿前 5 档深度的较小值，深度不足则扣分 |
| **资金费率** | 15% | `max(0, min(funding_diff × 100, 1.0))` | 0-100 | 做多所费率 - 做空所费率，正差得分，负差 0 分 |
| **波动率惩罚** | 10% | `max(0, 1 - volatility_penalty)` | 0-100 | 近期波动率越高，得分越低 |
| **流动性** | 10% | 固定值（主流币 80，小币种 50） | 0-100 | 24h 成交量排名 |
| **交易所稳定性** | 10% | 固定值（正常 90，延迟高 60） | 0-100 | API 延迟和近期宕机记录 |

#### 2.4.3 详细计算逻辑

**1. 价差收益因子 (35%)**

```python
def spread_factor(spread_pct, min_spread_pct=0.001):
    if spread_pct < min_spread_pct:
        return 0
    # 0.001 → 0 分, 0.006 → 100 分, >0.006 → 100 分
    return min((spread_pct - min_spread_pct) / 0.005, 1.0)
```

**临界场景**：
- 价差率 0.05% → 得分 0（低于最小阈值）
- 价差率 0.3% → 得分 (0.003 - 0.001) / 0.005 = 40 分
- 价差率 0.6% → 得分 100 分（满分）
- 价差率 1.0% → 得分 100 分（封顶）

**2. 深度/滑点因子 (20%)**

```python
def depth_factor(buy_depth, buy_price, sell_depth, sell_price, target_usdt=10000):
    buy_depth_usdt = buy_depth * buy_price  # 买一深度折算 USDT
    sell_depth_usdt = sell_depth * sell_price
    min_depth = min(buy_depth_usdt, sell_depth_usdt)
    # 深度 ≥ 目标仓位 → 100 分
    # 深度 = 目标仓位 50% → 50 分
    return min(min_depth / target_usdt, 1.0)
```

**临界场景**：
- 目标仓位 $10,000，两侧深度均 ≥ $10,000 → 100 分
- 目标仓位 $10,000，一侧深度仅 $5,000 → 50 分
- 目标仓位 $10,000，一侧深度仅 $1,000 → 10 分（高风险，大概率不触发）

**3. 资金费率因子 (15%)**

```python
def funding_factor(funding_rate_long_ex, funding_rate_short_ex):
    # funding_diff = 做多所的费率 - 做空所的费率
    # 正差：做多所费率更低（或做空所费率更高），持仓有额外收益
    # 负差：持仓有额外成本，得 0 分
    funding_diff = funding_rate_long_ex - funding_rate_short_ex
    return max(0, min(funding_diff * 100, 1.0))
```

**临界场景**：
- 做多所费率 0.01%，做空所费率 0.02% → diff = -0.01% → 0 分（持仓有成本）
- 做多所费率 0.01%，做空所费率 0.00% → diff = 0.01% → 100 分（满分）
- 做多所费率 0.00%，做空所费率 0.02% → diff = -0.02% → 0 分

**4. 波动率惩罚因子 (10%)**

```python
def volatility_factor(recent_volatility, threshold=0.02):
    # 近期波动率（如 5 分钟收益率标准差）
    # 波动率 < 2% → 90 分
    # 波动率 = 5% → 50 分
    # 波动率 > 10% → 0 分
    penalty = recent_volatility / threshold
    return max(0, 1 - penalty)
```

**临界场景**：
- 市场平静，波动率 0.5% → 得分 75 分
- 市场剧烈波动，波动率 5% → 得分 0 分（高风险，不触发）

**5. 流动性因子 (10%)**

- BTC/ETH/SOL 等主流币：80 分
- 24h 成交量 > $100M：80 分
- 24h 成交量 $10M-$100M：60 分
- 24h 成交量 < $10M：30 分

**6. 交易所稳定性因子 (10%)**

- 近期无异常，API 延迟 < 100ms：90 分
- API 延迟 100-300ms：70 分
- 近期有宕机记录：50 分

#### 2.4.4 综合示例

假设 BTC/USDT，Binance 做多，OKX 做空：

| 因子 | 权重 | 得分 | 加权得分 |
|------|------|------|----------|
| 价差收益 (0.3%) | 35% | 40 | 14.0 |
| 深度 ($8,000/$10,000) | 20% | 80 | 16.0 |
| 资金费率差 (0.005%) | 15% | 50 | 7.5 |
| 波动率 (1%) | 10% | 50 | 5.0 |
| 流动性 (BTC) | 10% | 80 | 8.0 |
| 交易所稳定性 | 10% | 90 | 9.0 |
| **总分** | | | **59.5** |

→ **不触发**（低于 80 分阈值）

调整参数后：

| 因子 | 权重 | 得分 | 加权得分 |
|------|------|------|----------|
| 价差收益 (0.8%) | 35% | 100 | 35.0 |
| 深度 ($12,000/$10,000) | 20% | 100 | 20.0 |
| 资金费率差 (0.01%) | 15% | 100 | 15.0 |
| 波动率 (0.5%) | 10% | 75 | 7.5 |
| 流动性 (BTC) | 10% | 80 | 8.0 |
| 交易所稳定性 | 10% | 90 | 9.0 |
| **总分** | | | **94.5** |

→ **触发**（高于 80 分阈值）

### 2.5 风控检查（下单前最后屏障）

即使信号满足上述所有条件，仍需通过以下风控检查：

| 检查项 | 规则 | 失败处理 |
|--------|------|----------|
| **余额检查** | 可用保证金 ≥ 预估占用 × 1.2 | 跳过该信号 |
| **持仓上限** | 当前持仓数 < max_positions | 跳过该信号 |
| **单所敞口** | 该交易所累计持仓 < max_exposure | 跳过该信号 |
| **黑名单** | 交易对/交易所不在黑名单中 | 跳过该信号 |
| **冷却期** | 同一交易对上次交易距今 > 60 秒 | 跳过该信号 |

### 2.6 下单执行策略

#### 2.6.1 下单顺序

**原则：先难后易**

1. **评估两侧流动性**：比较两侧订单簿深度
2. **先在流动性差的一侧下单**：确保难成交的一侧先成交
3. **再在流动性好的一侧下单**：容易成交，减少等待时间

**示例**：
- Binance 深度 $5,000，OKX 深度 $15,000
- 先在 Binance 下单（难成交），再在 OKX 下单（易成交）

#### 2.6.2 订单类型

| 阶段 | 订单类型 | 说明 |
|------|----------|------|
| **首次尝试** | `Post-Only Limit` (Maker) | 保证不吃单，手续费低 |
| **超时转单** | `IOC Limit` (Immediate or Cancel) | 立即成交或取消，不等待 |
| **紧急平仓** | `Market` | 不计成本，快速平仓 |

#### 2.6.3 下单流程

```
1. 计算目标仓位大小 (USDT)
2. 转换为币种数量 (size = amount / price)
3. 在流动性差的一侧下 Post-Only Limit 单
4. 等待成交（超时时间：3 秒）
5. 若成交 → 在另一侧下 Post-Only Limit 单
6. 若未成交 → 转 IOC 或取消
7. 记录两条腿的成交状态
```

#### 2.6.4 部分成交处理

**场景**：下单 0.1 BTC，实际成交 0.07 BTC

**处理**：
1. 按实际成交量调整另一侧的下单量
2. 若 Leg 1 成交 0.07 BTC → Leg 2 只下 0.07 BTC
3. 若 Leg 2 深度不足，仅成交 0.05 BTC → 按 0.05 BTC 计算最终仓位

---

## 3. 单边成交处理（最核心风控）

### 3.1 问题定义

**单边成交 (Leg Risk)**：一条腿成交，另一条腿未成交（或部分成交）。

此时系统持有**裸敞口**，价格反向波动将导致亏损。

### 3.2 处理策略

#### 3.2.1 Leg Timeout 机制

| 参数 | 默认值 | 说明 |
|------|--------|------|
| **leg_timeout** | 10 秒 | 一条腿成交后，等待另一条腿的最大时间 |
| **max_net_exposure** | $0 | 允许的最大净敞口（建议为 0） |

**流程**：
```
Leg 1 成交 → 启动计时器
    ↓
等待 Leg 2 成交
    ↓
├── Leg 2 在 timeout 内成交 → 正常对冲，进入持仓监控
    ↓
└── Leg 2 在 timeout 内未成交 → 紧急平仓 Leg 1
    ↓
    以市价单平仓 Leg 1
    ↓
    记录亏损，发送告警
```

#### 3.2.2 临界场景分析

**场景 A：Leg 1 成交，Leg 2 完全未成交**

- 操作：等待 10 秒后，市价平仓 Leg 1
- 亏损来源：Leg 1 成交价与平仓价之间的差额 + 手续费
- 预期亏损：通常较小（10 秒内价格波动有限）

**场景 B：Leg 1 成交 0.1 BTC，Leg 2 成交 0.05 BTC**

- 操作：Leg 2 仅部分成交，净敞口 0.05 BTC
- 选择 1：立即市价平仓 0.05 BTC 的 Leg 1 多余部分
- 选择 2：等待 Leg 2 继续成交（风险：价格可能反向波动）
- **默认策略**：立即平仓多余部分，确保净敞口为 0

**场景 C：Leg 1 成交后，价格剧烈波动**

- 操作：不等 timeout，立即市价平仓 Leg 1
- 触发条件：价格波动超过阈值（如 0.5%）
- 目的：减少亏损

**场景 D：两条腿都未成交**

- 操作：信号过期，丢弃
- 无亏损

**场景 E：下单后交易所 API 无响应**

- 操作：查询订单状态
- 若已成交 → 按正常流程处理
- 若未成交 → 取消订单，丢弃信号
- 若状态未知 → 等待后重试查询，仍未知则紧急处理

### 3.3 紧急平仓策略

**原则：宁可亏损，不可裸奔**

| 条件 | 操作 |
|------|------|
| Leg timeout 到期 | 市价平仓已成交腿 |
| 价格波动 > 0.5% | 市价平仓已成交腿 |
| API 断线 > 5 秒 | 暂停新单，检查已有头寸 |
| 单所敞口 > max_net_exposure | 市价平仓超出部分 |

---

## 4. 自动平仓策略

### 4.1 总体原则

持仓建立后，系统持续监控以下条件，**任一条件触发即平仓**：

### 4.2 止盈条件

#### 4.2.1 价差收敛止盈

**触发条件**：`当前价差率 ≤ take_profit_spread`（默认 0.02%）

**逻辑**：
- 建仓时价差率为 0.3%，平仓时价差率收敛至 0.02%
- 利润 = (0.3% - 0.02%) × 仓位 - 手续费
- 仓位 $10,000 → 利润 ≈ $28 - $8 = $20

**临界场景**：
- 价差率快速收敛 → 立即平仓，锁定利润
- 价差率缓慢收敛 → 持续监控，直到触及阈值
- 价差率收敛后又扩大 → 若已触发阈值则已平仓；若未触发则继续等待

#### 4.2.2 固定利润止盈

**触发条件**：`当前未实现利润 ≥ take_profit_usdt`（默认 $5）

**逻辑**：
- 实时计算未实现盈亏
- 达到目标利润后立即平仓
- 适用于价差收敛较慢但已积累足够利润的场景

**计算方式**：
```
PnL = (current_buy_price - entry_buy_price) × size 
    + (entry_sell_price - current_sell_price) × size
    - 手续费
```

### 4.3 止损条件

#### 4.3.1 价差扩大止损

**触发条件**：`当前价差率 ≥ entry_spread% + stop_loss_spread`（默认 entry + 0.5%）

**逻辑**：
- 建仓时价差率为 0.3%，止损阈值为 0.8%
- 价差反向扩大，意味着亏损增加
- 仓位 $10,000 → 亏损 ≈ $50 + 手续费

**临界场景**：
- 价差率瞬间跳变（如交易所价格异常）→ 立即平仓
- 价差率缓慢扩大 → 持续监控，触及阈值即平仓

#### 4.3.2 固定亏损止损

**触发条件**：`当前未实现亏损 ≥ stop_loss_usdt`（默认 $10）

**逻辑**：
- 实时计算未实现盈亏
- 亏损达到阈值后立即平仓
- 与价差扩大止损互为补充

#### 4.3.3 最大持仓时间止损

**触发条件**：`持仓时间 ≥ max_hold_time`（默认 2 小时）

**逻辑**：
- 套利策略预期价差在短期内回归
- 若长时间未回归，说明逻辑可能失效
- 避免资金占用和资金费率磨损

**临界场景**：
- 持仓 1.9 小时，价差即将收敛 → 继续等待
- 持仓 2 小时，价差未收敛 → 强制平仓
- 持仓 2 小时，价差已扩大 → 强制平仓（可能亏损）

#### 4.3.4 资金费率反转平仓

**触发条件**：`funding_rate_diff < 0`（做多所费率 > 做空所费率）

**逻辑**：
- 建仓时预期资金费率差为正（持仓有额外收益）
- 若费率差反转，持仓变为有成本
- 立即平仓，避免持续亏损

**临界场景**：
- 费率差在 0 附近波动 → 可设置 hysteresis（滞后），如 -0.001% 才触发
- 费率差突然反转 → 立即平仓

### 4.4 平仓执行

#### 4.4.1 平仓顺序

**原则：同时平仓**

1. 在两侧同时下市价单
2. 若一侧市价单未完全成交 → 重试或 IOC
3. 记录实际成交价和盈亏

#### 4.4.2 平仓流程

```
1. 检测到平仓条件触发
2. 记录触发原因（止盈/止损/超时/费率反转）
3. 在两侧同时下市价平仓单
4. 等待成交（超时时间：5 秒）
5. 若成交 → 更新持仓状态，记录盈亏
6. 若未成交 → 重试或紧急处理
7. 发送平仓通知
```

### 4.5 平仓条件优先级

| 优先级 | 条件 | 说明 |
|--------|------|------|
| P0 | 单边成交 timeout | 立即执行，不可延迟 |
| P0 | 价格剧烈波动 | 立即执行，不可延迟 |
| P1 | 固定亏损止损 | 快速执行 |
| P1 | 价差扩大止损 | 快速执行 |
| P2 | 最大持仓时间 | 定时检查 |
| P2 | 资金费率反转 | 定时检查 |
| P3 | 价差收敛止盈 | 定时检查 |
| P3 | 固定利润止盈 | 定时检查 |

---

## 5. 异常处理与边界场景

### 5.1 交易所异常

| 场景 | 检测方式 | 处理策略 |
|------|----------|----------|
| **API 超时** | 请求超过 5 秒无响应 | 重试 2 次，仍失败则暂停该交易所 |
| **API 返回错误** | HTTP 4xx/5xx | 记录错误，暂停 30 秒后重试 |
| **WebSocket 断线** | 心跳超时 | 自动重连，重连期间暂停交易 |
| **交易所宕机** | 连续多次请求失败 | 暂停所有涉及该交易所的交易，告警 |
| **拔网线/限制交易** | 订单长时间未成交 | 紧急平仓另一侧，告警 |

### 5.2 系统异常

| 场景 | 检测方式 | 处理策略 |
|------|----------|----------|
| **进程崩溃** | 健康检查失败 | 自动重启，恢复后检查持仓状态 |
| **数据库断连** | 查询失败 | 暂停交易，重连后恢复 |
| **内存溢出** | 内存使用率 > 90% | 告警，重启进程 |

### 5.3 极端行情

| 场景 | 检测方式 | 处理策略 |
|------|----------|----------|
| **闪崩/暴涨** | 价格 1 分钟内波动 > 5% | 暂停新单，检查已有持仓 |
| **流动性枯竭** | 订单簿深度 < $1,000 | 暂停该交易对交易 |
| **资金费率异常** | 费率 > 0.1% | 告警，考虑提前平仓 |

### 5.4 边界场景 Checklist

- [ ] 信号触发时，余额刚好不足 → 跳过
- [ ] 信号触发时，持仓数刚好达到上限 → 跳过
- [ ] Leg 1 成交后，Leg 2 交易所宕机 → timeout 后平仓 Leg 1
- [ ] 平仓时，一侧市价单滑点极大 → 记录实际滑点，告警
- [ ] 持仓期间，资金费率调整 → 重新评估是否继续持仓
- [ ] 持仓期间，交易所调整保证金规则 → 检查保证金是否充足
- [ ] 持仓期间，币种下架 → 紧急平仓
- [ ] 系统重启后，发现未记录的持仓 → 从交易所同步持仓状态

---

## 6. 参数调优建议

### 6.1 保守型（推荐初期使用）

| 参数 | 值 | 说明 |
|------|-----|------|
| 最低评分 | 85 | 仅高质量信号 |
| 最小价差 | 0.15% | 较大利润空间 |
| 信号持续时长 | 15 秒 | 更严格的信号确认 |
| 最大并发持仓 | 2 | 降低风险暴露 |
| 单笔金额 | $200 | 小仓位试水 |
| 止盈价差 | 0.03% | 较快止盈 |
| 止损价差 | 0.3% | 较紧止损 |
| 最大持仓时间 | 1 小时 | 快速进出 |
| Leg Timeout | 5 秒 | 快速处理单边成交 |

### 6.2 均衡型（默认配置）

| 参数 | 值 | 说明 |
|------|-----|------|
| 最低评分 | 80 | 平衡信号数量与质量 |
| 最小价差 | 0.1% | 标准利润空间 |
| 信号持续时长 | 10 秒 | 标准信号确认 |
| 最大并发持仓 | 3 | 适度分散 |
| 单笔金额 | $500 | 标准仓位 |
| 止盈价差 | 0.02% | 标准止盈 |
| 止损价差 | 0.5% | 标准止损 |
| 最大持仓时间 | 2 小时 | 标准持仓时间 |
| Leg Timeout | 10 秒 | 标准 timeout |

### 6.3 激进型（有经验后使用）

| 参数 | 值 | 说明 |
|------|-----|------|
| 最低评分 | 70 | 更多交易机会 |
| 最小价差 | 0.05% | 较小利润空间 |
| 信号持续时长 | 5 秒 | 快速响应 |
| 最大并发持仓 | 5 | 更多持仓 |
| 单笔金额 | $1,000 | 大仓位 |
| 止盈价差 | 0.01% | 快速止盈 |
| 止损价差 | 0.8% | 较宽止损 |
| 最大持仓时间 | 4 小时 | 更长持仓时间 |
| Leg Timeout | 15 秒 | 更长等待时间 |

---

## 7. 待专家评审的问题

### 7.1 打分模型

1. **权重分配是否合理？** 价差收益 35% 是否过高/过低？
2. **因子计算是否准确？** 深度因子用前 5 档深度是否足够？
3. **是否需要加入更多因子？** 如：交易对相关性、历史价差回归速度等
4. **评分阈值 80 分是否合理？** 是否需要动态调整？

### 7.2 下单策略

1. **信号持续时长 10 秒是否合理？** 是否太短（假突破）或太长（错过机会）？
2. **下单顺序"先难后易"是否正确？** 是否有更好的策略？
3. **Post-Only → IOC → Market 的转单策略是否合理？** 超时时间如何设置？
4. **部分成交的处理是否完善？** 是否有更好的对冲方式？

### 7.3 平仓策略

1. **止盈/止损阈值是否合理？** 是否需要动态调整（如基于 ATR）？
2. **最大持仓时间 2 小时是否合理？** 是否需要根据币种调整？
3. **资金费率反转平仓是否必要？** 是否过于敏感？
4. **平仓时是否应该区分止盈和止损？** 止盈用限价，止损用市价？

### 7.4 风控

1. **Leg Timeout 10 秒是否合理？** 是否太短（误杀）或太长（风险暴露）？
2. **最大净敞口 $0 是否过于严格？** 是否允许小幅敞口（如 $100）？
3. **是否需要加入全局止损？** 如日亏损 > $100 则停止交易
4. **是否需要加入仓位相关性检查？** 避免同时持有高度相关的套利对

### 7.5 其他

1. **是否需要考虑交易所手续费差异？** Maker/Taker 费率不同
2. **是否需要考虑资金费率结算时间？** 每 8 小时结算，持仓时机很重要
3. **是否需要加入回测模块？** 验证策略历史表现
4. **是否需要加入模拟交易模式？** 实盘前先用模拟验证

---

## 8. 附录

### 8.1 术语表

| 术语 | 说明 |
|------|------|
| **Leg** | 套利交易的一条腿（一侧交易所的订单） |
| **对冲** | 同时做多和做空，抵消方向性风险 |
| **裸敞口** | 未对冲的净头寸 |
| **Maker** | 挂单方，手续费较低 |
| **Taker** | 吃单方，手续费较高 |
| **Post-Only** | 仅挂单，确保不吃单 |
| **IOC** | Immediate or Cancel，立即成交或取消 |
| **资金费率** | 永续合约每 8 小时支付的费用 |
| **ATR** | Average True Range，平均真实波幅 |

### 8.2 参考公式

**价差率**：
```
Spread% = (Price_Sell - Price_Buy) / Price_Buy
```

**预估利润**：
```
Profit = Spread% × Position_Size - Fee_Buy - Fee_Sell - Funding_Cost
```

**手续费**：
```
Fee = Position_Size × Fee_Rate
```

**资金费率成本**：
```
Funding_Cost = Position_Size × Funding_Rate × (Hold_Hours / 8)
```

**未实现盈亏**：
```
PnL = (Current_Buy - Entry_Buy) × Size + (Entry_Sell - Current_Sell) × Size - Fees
```

---

**文档结束**

请专家重点评审：打分模型权重、信号持续时长、Leg Timeout 机制、止盈止损阈值、以及边界场景处理是否完善。
