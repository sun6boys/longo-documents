# Longo 跨所套利自动交易策略文档

**版本**: v2.0  
**日期**: 2026-05-21  
**状态**: 专家重新设计  

---

## 1. 策略总览

### 1.1 核心逻辑

同一币种永续合约在不同交易所（Binance、OKX、Bybit）之间存在价差。在低价所做多、高价所做空，建立 Delta 中性对冲头寸，等待价差收敛后同时平仓获利。

```
[行情采集] → [价差计算] → [统计分析] → [信号评估] → [信号确认] → [风控检查] → [下单执行]
     ↑                                                                            ↓
     └─────────────────────── 持续监控 ←──────────── [持仓管理] ←──────── [状态同步]
```

### 1.2 关键设计原则

1. **安全第一**：任何不确定状态，默认选择平仓或不开仓
2. **成本敏感**：所有决策必须考虑实际手续费（区分 Maker/Taker）和滑点
3. **速度优先**：套利窗口稍纵即逝，下单环节以速度为核心
4. **状态可追溯**：每个决策点记录完整上下文，便于事后分析

### 1.3 基础设施前提

| 配置项 | 要求 | 说明 |
|--------|------|------|
| **保证金模式** | 逐仓（Isolated） | 隔离风险，单个仓位爆仓不影响其他 |
| **杠杆倍数** | 5x-10x（建议 5x） | 低杠杆降低爆仓风险，留充足保证金缓冲 |
| **交易对白名单** | BTC、ETH、SOL 等主流币 | 流动性充足的交易对才纳入监控 |
| **API 延迟要求** | < 200ms | 超过则降低该交易所的信号权重 |
| **资金分配** | 每所预留总资金的 30-40% | 留 10-20% 作为安全缓冲 |

---

## 2. 价差分析引擎

### 2.1 价差计算

**基础价差率**：
```
raw_spread = (ask_price_high_exchange - bid_price_low_exchange) / bid_price_low_exchange
```

注意：用 **ask 价**（卖一）和 **bid 价**（买一），因为实际成交时做多要吃 ask、做空要吃 bid。这比用 mid price 更真实。

**净价差率**（扣除预估成本）：
```python
def net_spread(ask_high, bid_low, fee_long, fee_short):
    """
    ask_high:  高价所卖一价（我们在此做空，挂单则用 bid）
    bid_low:   低价所买一价（我们在此做多，挂单则用 ask）
    fee_long:  做多所单边手续费率
    fee_short: 做空所单边手续费率
    """
    raw = (ask_high - bid_low) / bid_low
    # 开仓双边手续费
    open_cost = fee_long + fee_short
    # 预估平仓双边手续费（保守用 Taker 费率）
    close_cost = fee_long + fee_short
    net = raw - open_cost - close_cost
    return net
```

### 2.2 价差统计分析（Z-Score 模型）

原文档使用固定阈值判断价差是否"足够大"，这不够科学。不同交易对、不同时段的价差分布不同。引入 Z-Score 模型，用统计方法判断当前价差是否偏离均值：

```python
class SpreadAnalyzer:
    def __init__(self, window=500, min_samples=100):
        """
        window: 滑动窗口大小（价差采样点数）
        min_samples: 最少采样数，不足则不计算 Z-Score
        """
        self.window = window
        self.min_samples = min_samples
        self.spread_history = deque(maxlen=window)

    def update(self, spread_pct: float):
        self.spread_history.append(spread_pct)

    def z_score(self) -> float | None:
        if len(self.spread_history) < self.min_samples:
            return None
        mean = np.mean(self.spread_history)
        std = np.std(self.spread_history)
        if std < 1e-8:  # 标准差过小，价差无波动
            return 0.0
        return (self.spread_history[-1] - mean) / std

    def half_life(self) -> float | None:
        """
        计算价差均值回归半衰期（OU 过程）
        半衰期越短，价差回归越快，套利机会越好
        """
        if len(self.spread_history) < self.min_samples:
            return None
        spreads = np.array(self.spread_history)
        lag = spreads[:-1]
        delta = spreads[1:] - spreads[:-1]
        # OLS: delta = alpha + beta * lag + epsilon
        beta = np.cov(delta, lag)[0][1] / np.var(lag)
        if beta >= 0:  # 不存在均值回归
            return float('inf')
        return -np.log(2) / beta
```

**Z-Score 在策略中的应用**：

| Z-Score 值 | 含义 | 策略动作 |
|-------------|------|----------|
| < 1.0 | 价差正常 | 不操作 |
| 1.0 - 1.5 | 轻度偏离 | 关注，不触发 |
| 1.5 - 2.5 | 显著偏离 | 进入信号评估 |
| 2.5 - 3.5 | 强烈偏离 | 高质量信号 |
| > 3.5 | 极端偏离 | 可能是结构性变化，谨慎对待 |

---

## 3. 开仓信号评估模型

### 3.1 双层过滤架构

```
第一层：硬性门槛（不满足直接跳过，不计算评分）
    ↓ 通过
第二层：多因子打分模型（综合评估信号质量）
    ↓ 评分 ≥ 阈值
信号确认 → 风控检查 → 下单
```

### 3.2 第一层：硬性门槛

所有条件必须同时满足，任一不满足则直接跳过，不进入打分：

| 门槛 | 默认值 | 说明 |
|------|--------|------|
| 自动交易开关 | OFF | 总开关 |
| 净价差率 > 0 | - | 扣除所有成本后仍有正收益空间 |
| Z-Score ≥ 最小值 | 1.5 | 价差统计性显著偏离 |
| 半衰期 < 最大值 | 300 秒 | 价差有回归趋势，且回归速度可接受 |
| 并发持仓 < 上限 | 3 | 同一交易对不重复开仓 |
| 全局风控未触发 | - | 日/周亏损、回撤未达上限 |
| 两所 API 均正常 | - | 延迟 < 500ms，无错误 |

### 3.3 第二层：多因子打分模型

#### 3.3.1 公式

```
Score = Σ(Factor_i × Weight_i)
```

每个因子输出 0-100 分，加权后总分也是 0-100 分。

#### 3.3.2 因子详解

| 因子 | 权重 | 说明 |
|------|------|------|
| **净收益空间** | 30% | 扣除手续费后的实际利润空间 |
| **价差 Z-Score** | 20% | 统计偏离度，越大说明回归概率越高 |
| **滑点风险** | 20% | 基于订单簿深度的真实滑点估算 |
| **均值回归速度** | 10% | 半衰期越短越好 |
| **波动率环境** | 10% | 当前市场波动率是否适合套利 |
| **资金费率增益** | 10% | 持仓期间资金费率是否有利 |

#### 3.3.3 各因子计算

**1. 净收益空间因子 (30%)**

```python
def net_profit_factor(net_spread_pct):
    """
    net_spread_pct: 净价差率（已扣除双边开平仓手续费）
    
    0%      → 0 分（刚好覆盖成本）
    0.05%   → 50 分
    0.10%   → 100 分（满分）
    >0.10%  → 100 分（封顶）
    """
    if net_spread_pct <= 0:
        return 0
    return min(net_spread_pct / 0.001, 100)  # 0.001 = 0.1%
```

**2. 价差 Z-Score 因子 (20%)**

```python
def z_score_factor(z_score):
    """
    z_score: 当前价差的 Z-Score 值
    
    < 1.5   → 0 分（不显著）
    1.5     → 0 分
    2.0     → 33 分
    3.0     → 100 分（满分）
    > 3.5   → 80 分（极端值可能是结构性变化，反而降分）
    > 5.0   → 50 分（很可能是异常数据）
    """
    if z_score < 1.5:
        return 0
    if z_score > 5.0:
        return 50  # 极端异常，降权
    if z_score > 3.5:
        return 80  # 过高也有风险
    # 1.5 → 0, 3.0 → 100
    return min((z_score - 1.5) / 1.5 * 100, 100)
```

**3. 滑点风险因子 (20%)**

不再简单看"前 5 档深度总量"，而是模拟实际吃单的加权平均滑点：

```python
def slippage_factor(orderbook_bids, orderbook_asks, target_usdt):
    """
    模拟在订单簿上吃掉 target_usdt 金额的滑点
    orderbook_bids: [(price, qty), ...] 买方挂单（做空时吃 bid）
    orderbook_asks: [(price, qty), ...] 卖方挂单（做多时吃 ask）
    target_usdt: 目标仓位 USDT
    
    返回 0-100 分，滑点越小分越高
    """
    def estimate_slippage(orders, target, is_buy):
        """估算吃掉 target USDT 的加权平均价格偏移"""
        filled = 0
        cost = 0
        best_price = orders[0][0] if orders else 0
        for price, qty in orders:
            fill_qty = min(qty, (target - filled) / price)
            cost += fill_qty * price
            filled += fill_qty * price
            if filled >= target:
                break
        if filled < target:
            return 1.0  # 深度不足，最大滑点
        avg_price = cost / (filled / best_price) if best_price > 0 else 0
        return abs(avg_price - best_price) / best_price

    slippage_long = estimate_slippage(orderbook_asks, target_usdt, is_buy=True)
    slippage_short = estimate_slippage(orderbook_bids, target_usdt, is_buy=False)
    total_slippage = slippage_long + slippage_short

    # 滑点 0% → 100 分, 0.05% → 50 分, ≥ 0.1% → 0 分
    if total_slippage >= 0.001:  # 0.1%
        return 0
    return max(0, (1 - total_slippage / 0.001) * 100)
```

**4. 均值回归速度因子 (10%)**

```python
def mean_reversion_factor(half_life_seconds):
    """
    half_life_seconds: 价差均值回归的半衰期（秒）
    
    < 30 秒    → 100 分（快速回归）
    60 秒      → 80 分
    120 秒     → 60 分
    300 秒     → 0 分（回归太慢）
    inf        → 0 分（不存在回归）
    """
    if half_life_seconds is None or half_life_seconds == float('inf'):
        return 0
    if half_life_seconds <= 30:
        return 100
    if half_life_seconds >= 300:
        return 0
    # 30 → 100, 300 → 0, 线性插值
    return max(0, (300 - half_life_seconds) / 270 * 100)
```

**5. 波动率环境因子 (10%)**

```python
def volatility_factor(recent_volatility_pct):
    """
    recent_volatility_pct: 过去 1 小时的价格波动率
    （用 1 分钟收益率的标准差计算，取 60 个样本）
    
    计算方式: std(ln(close_i / close_{i-1})) for i in last 60 minutes
    
    < 0.3%  → 100 分（非常平静）
    0.5%    → 80 分（平静）
    1.0%    → 50 分（正常波动）
    2.0%    → 0 分（高波动，不适合套利）
    > 2.0%  → 0 分
    """
    if recent_volatility_pct >= 0.02:
        return 0
    if recent_volatility_pct <= 0.003:
        return 100
    # 0.003 → 100, 0.02 → 0
    return max(0, (0.02 - recent_volatility_pct) / 0.017 * 100)
```

**6. 资金费率增益因子 (10%)**

```python
def funding_rate_factor(funding_rate_long_ex, funding_rate_short_ex):
    """
    资金费率方向说明：
    - 永续合约中，正费率 → 多头付给空头
    - 负费率 → 空头付给多头
    
    我们做多 A 所、做空 B 所：
    - A 所费率 > 0 → 我们（多头）要付钱 → 不利
    - B 所费率 > 0 → 我们（空头）收钱 → 有利
    
    benefit = B 所费率 - A 所费率
    正值 = 我们持仓有额外收益
    负值 = 我们持仓有额外成本
    """
    benefit = funding_rate_short_ex - funding_rate_long_ex

    # benefit ≤ -0.01% → 0 分（有显著成本）
    # benefit = 0       → 50 分（中性）
    # benefit ≥ 0.01%   → 100 分（有显著收益）
    if benefit <= -0.0001:
        return 0
    if benefit >= 0.0001:
        return 100
    # -0.01% → 0, 0 → 50, 0.01% → 100
    return (benefit + 0.0001) / 0.0002 * 100
```

#### 3.3.4 综合评分示例

BTC/USDT，Binance 做多，OKX 做空：

**场景 A：不触发**

| 因子 | 权重 | 原始分 | 加权分 | 数据 |
|------|------|--------|--------|------|
| 净收益空间 | 30% | 30 | 9.0 | 净价差 0.03% |
| Z-Score | 20% | 33 | 6.6 | Z = 2.0 |
| 滑点风险 | 20% | 60 | 12.0 | 总滑点 0.04% |
| 均值回归速度 | 10% | 45 | 4.5 | 半衰期 170s |
| 波动率环境 | 10% | 50 | 5.0 | 波动率 1.0% |
| 资金费率增益 | 10% | 50 | 5.0 | benefit = 0 |
| **总分** | | | **42.1** | **不触发** |

**场景 B：触发**

| 因子 | 权重 | 原始分 | 加权分 | 数据 |
|------|------|--------|--------|------|
| 净收益空间 | 30% | 80 | 24.0 | 净价差 0.08% |
| Z-Score | 20% | 100 | 20.0 | Z = 3.0 |
| 滑点风险 | 20% | 90 | 18.0 | 总滑点 0.01% |
| 均值回归速度 | 10% | 80 | 8.0 | 半衰期 60s |
| 波动率环境 | 10% | 80 | 8.0 | 波动率 0.5% |
| 资金费率增益 | 10% | 100 | 10.0 | benefit = 0.01% |
| **总分** | | | **88.0** | **触发** |

---

## 4. 信号确认机制

### 4.1 采样确认法（替代原连续时长法）

原文档要求信号"连续维持 10 秒"，问题是：
- 10 秒在加密市场太长，好的套利窗口往往几秒就关闭
- "连续"要求过于严格，一个 tick 的波动就会重置

**新方案：窗口采样确认**

```python
class SignalConfirmer:
    def __init__(self,
                 window_seconds=3,        # 确认窗口
                 sample_interval_ms=200,   # 采样间隔
                 confirm_ratio=0.7,        # 确认比例
                 min_score=75,             # 最低评分阈值
                 max_window_seconds=10):   # 最大等待窗口（超过则过期）
        self.window_seconds = window_seconds
        self.sample_interval_ms = sample_interval_ms
        self.confirm_ratio = confirm_ratio
        self.min_score = min_score
        self.max_window_seconds = max_window_seconds
        self.samples = []
        self.first_signal_time = None

    def add_sample(self, score: float, net_spread: float, z_score: float, timestamp: float):
        """每次采样时调用"""
        if self.first_signal_time is None:
            self.first_signal_time = timestamp

        # 清除窗口外的旧样本
        cutoff = timestamp - self.window_seconds
        self.samples = [s for s in self.samples if s['time'] >= cutoff]

        self.samples.append({
            'time': timestamp,
            'score': score,
            'net_spread': net_spread,
            'z_score': z_score,
            'qualified': score >= self.min_score and net_spread > 0 and z_score >= 1.5
        })

    def is_confirmed(self, current_time: float) -> bool:
        """检查信号是否已确认"""
        # 过期检查
        if self.first_signal_time and (current_time - self.first_signal_time) > self.max_window_seconds:
            self.reset()
            return False

        # 样本数量检查
        expected_samples = self.window_seconds * 1000 / self.sample_interval_ms
        if len(self.samples) < expected_samples * 0.5:
            return False  # 样本不足

        qualified_count = sum(1 for s in self.samples if s['qualified'])
        return qualified_count / len(self.samples) >= self.confirm_ratio

    def get_confirm_snapshot(self) -> dict:
        """确认时记录快照，用于下单前二次校验"""
        scores = [s['score'] for s in self.samples if s['qualified']]
        spreads = [s['net_spread'] for s in self.samples if s['qualified']]
        return {
            'avg_score': np.mean(scores) if scores else 0,
            'avg_spread': np.mean(spreads) if spreads else 0,
            'min_spread': min(spreads) if spreads else 0,
            'sample_count': len(self.samples),
            'qualified_ratio': sum(1 for s in self.samples if s['qualified']) / len(self.samples)
        }

    def reset(self):
        self.samples.clear()
        self.first_signal_time = None
```

**核心参数说明**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| window_seconds | 3 秒 | 确认窗口长度 |
| sample_interval_ms | 200ms | 每 200ms 采样一次，3 秒窗口内约 15 个样本 |
| confirm_ratio | 70% | 窗口内 70% 的样本满足条件即确认 |
| min_score | 75 分 | 单次采样的最低评分 |
| max_window_seconds | 10 秒 | 首次信号出现后 10 秒内未确认则过期 |

### 4.2 下单前二次校验

信号确认后、实际下单前，必须再次校验当前实时状态：

```python
def pre_order_validation(current_spread, confirm_snapshot, current_score):
    """
    下单前二次校验
    防止信号确认后到下单之间价差已经变化
    """
    # 1. 当前净价差不低于确认期间最低值的 50%
    if current_spread < confirm_snapshot['min_spread'] * 0.5:
        return False, "价差已大幅收敛"

    # 2. 当前评分不低于确认期间平均分的 70%
    if current_score < confirm_snapshot['avg_score'] * 0.7:
        return False, "评分已大幅下降"

    return True, "通过"
```

---

## 5. 风控检查（下单前最后屏障）

信号确认后，下单前必须通过全部风控检查：

| 检查项 | 规则 | 失败处理 |
|--------|------|----------|
| **余额检查** | 可用保证金 ≥ 预估占用 × 1.5（含安全缓冲） | 跳过 |
| **持仓上限** | 当前持仓数 < max_positions | 跳过 |
| **单所敞口** | 该交易所累计持仓 < max_exchange_exposure | 跳过 |
| **同交易对检查** | 同一交易对同方向无已有持仓 | 跳过 |
| **冷却期** | 同一交易对上次平仓距今 > cooldown_seconds | 跳过 |
| **黑名单** | 交易对/交易所不在黑名单中 | 跳过 |
| **全局风控** | 日/周亏损、连续亏损、总回撤均未达上限 | 暂停所有交易 |
| **两所 API 健康** | 两个交易所 API 延迟 < 500ms 且近 1 分钟无错误 | 跳过 |

---

## 6. 下单执行策略

### 6.1 核心原则：同时下单，快速成交

套利的本质是锁定价差。**两腿之间的时间差就是风险**。因此：

- 放弃 Post-Only 策略（Maker 费率低但成交不确定，不适合套利）
- 两腿尽可能同时发出，用 Limit IOC 保证即时成交或立即取消

### 6.2 仓位计算

```python
def calculate_position_size(
    available_margin_long,    # 做多所可用保证金
    available_margin_short,   # 做空所可用保证金
    leverage,                 # 杠杆倍数
    max_position_usdt,        # 单笔最大仓位
    min_depth_usdt,           # 两侧订单簿最小深度
    price                     # 当前价格
):
    """
    仓位大小 = min(各项限制)
    """
    # 1. 保证金限制（取两所中较小值，留 50% 安全缓冲）
    margin_limit = min(available_margin_long, available_margin_short) * leverage * 0.5

    # 2. 配置限制
    config_limit = max_position_usdt

    # 3. 深度限制（不超过最小深度的 30%，避免过度冲击）
    depth_limit = min_depth_usdt * 0.3

    position_usdt = min(margin_limit, config_limit, depth_limit)

    # 转换为币种数量
    size = position_usdt / price

    return size, position_usdt
```

### 6.3 下单流程

```
1. 计算仓位大小
2. 构建两腿订单：
   - Leg Long:  在低价所下 Limit IOC 买入开多，价格 = ask_1 + 1 tick（给 1 tick 滑点容忍）
   - Leg Short: 在高价所下 Limit IOC 卖出开空，价格 = bid_1 - 1 tick（给 1 tick 滑点容忍）
3. 同时发送两笔订单（并发请求）
4. 等待两笔订单结果（超时 3 秒）
5. 汇总成交情况：
   ├── 两腿均完全成交 → 记录持仓，进入持仓管理
   ├── 两腿均未成交   → 信号过期，无操作
   ├── 一腿成交一腿未成交 → 进入单边成交处理
   └── 部分成交       → 进入部分成交处理
```

### 6.4 部分成交处理

```python
def handle_partial_fill(leg1_filled, leg2_filled, leg1_target, leg2_target):
    """
    处理两腿成交量不一致的情况
    
    原则：以较小成交量为准，多余部分立即平掉
    """
    matched_size = min(leg1_filled, leg2_filled)

    if matched_size == 0:
        # 只有一腿成交 → 完全单边，进入紧急处理
        return 'single_leg'

    # 两腿都有成交但量不同
    excess_leg1 = leg1_filled - matched_size
    excess_leg2 = leg2_filled - matched_size

    if excess_leg1 > 0:
        # Leg1 多余，立即市价平掉多余部分
        market_close(exchange='long_ex', size=excess_leg1, side='close_long')

    if excess_leg2 > 0:
        # Leg2 多余，立即市价平掉多余部分
        market_close(exchange='short_ex', size=excess_leg2, side='close_short')

    if matched_size < leg1_target * 0.3:
        # 匹配量太小（不到目标的 30%），不值得持仓，全部平掉
        market_close(exchange='long_ex', size=matched_size, side='close_long')
        market_close(exchange='short_ex', size=matched_size, side='close_short')
        return 'too_small_cancelled'

    # 按匹配量建立持仓
    return 'partial_position', matched_size
```

---

## 7. 单边成交处理（Leg Risk）

### 7.1 问题定义

一条腿成交而另一条腿未成交（或部分成交），系统持有裸敞口。

**开仓和平仓阶段都存在此风险。**

### 7.2 开仓阶段：单边成交处理

```
Leg 1 成交，Leg 2 未成交
    ↓
启动紧急计时器 + 实时价格监控
    ↓ 同时进行
├── [路径 A] 在 Leg 2 重试（最多 2 次，用更激进的价格）
│   ├── 重试成功 → 正常对冲完成
│   └── 重试失败 → 进入路径 C
│
├── [路径 B] 价格波动监控
│   └── 裸敞口亏损 > emergency_loss_limit → 立即市价平仓 Leg 1
│
└── [路径 C] 超时（leg_timeout 到期，默认 3 秒）
    └── 取消 Leg 2 所有挂单 → 市价平仓 Leg 1
```

### 7.3 关键参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| leg_timeout | 3 秒 | 一腿成交后等待另一腿的最大时间 |
| emergency_loss_pct | 0.3% | 裸敞口亏损超过此比例立即平仓，不等 timeout |
| leg_retry_count | 2 | Leg 2 最大重试次数 |
| leg_retry_price_offset | 2 ticks | 每次重试增加的价格偏移 |

### 7.4 开仓单边成交的代码逻辑

```python
async def handle_open_single_leg(
    filled_leg: dict,      # 已成交腿信息 {exchange, side, size, price}
    failed_leg: dict,      # 未成交腿信息 {exchange, side, target_size}
    leg_timeout: float = 3.0,
    emergency_loss_pct: float = 0.003,
    max_retries: int = 2
):
    """
    处理开仓阶段的单边成交
    """
    start_time = time.time()
    entry_price = filled_leg['price']
    retry_count = 0

    while time.time() - start_time < leg_timeout:
        # 1. 检查价格波动是否触发紧急平仓
        current_price = get_current_price(filled_leg['exchange'], filled_leg['symbol'])
        unrealized_loss_pct = abs(current_price - entry_price) / entry_price

        if unrealized_loss_pct >= emergency_loss_pct:
            # 紧急平仓，不等 timeout
            log.warning(f"裸敞口亏损 {unrealized_loss_pct:.4%}，紧急平仓")
            await market_close(filled_leg)
            return 'emergency_closed', unrealized_loss_pct

        # 2. 重试 Leg 2
        if retry_count < max_retries:
            retry_count += 1
            offset = retry_count * 2  # 每次增加 2 ticks
            result = await retry_leg2_with_offset(failed_leg, offset)
            if result.filled:
                return 'retry_success', result.filled_size

        await asyncio.sleep(0.2)  # 200ms 间隔检查

    # 3. 超时，强制平仓
    log.warning(f"Leg 2 超时未成交，强制平仓 Leg 1")
    cancel_result = await cancel_all_orders(failed_leg['exchange'], failed_leg['symbol'])
    close_result = await market_close(filled_leg)

    return 'timeout_closed', close_result
```

### 7.5 平仓阶段：单边成交处理

平仓时两腿同时下市价单。若一腿成交另一腿未成交：

```
平仓 Leg 1 成功，平仓 Leg 2 失败
    ↓
此时状态：Leg 2 的原始持仓仍然存在（裸敞口）
    ↓
立即对 Leg 2 重试市价平仓（最多 3 次，每次间隔 500ms）
    ├── 重试成功 → 正常结束
    └── 连续 3 次失败 → 
        ├── 检查交易所 API 状态
        ├── 若 API 正常但订单被拒 → 调整价格重试
        └── 若 API 异常 → 记录告警，启动手动干预流程
```

注意：平仓单边成交比开仓更危险，因为此时系统已经失去了对冲保护。平仓阶段**不设 timeout 等待，失败就立即重试**。

---

## 8. 持仓管理与平仓策略

### 8.1 持仓监控频率

| 监控项 | 频率 | 说明 |
|--------|------|------|
| 价差变化 | 200ms | 实时计算当前价差和未实现盈亏 |
| 止盈/止损检查 | 200ms | 与价差同步检查 |
| 资金费率 | 60 秒 | 费率变化较慢，不需要高频 |
| 持仓时间 | 1 秒 | 定时检查是否超过最大持仓时间 |
| 交易所健康 | 5 秒 | API 延迟和状态 |

### 8.2 平仓条件总览

```
平仓条件（任一触发即平仓）：

紧急级（P0，立即执行，不可延迟）：
├── 交易所 API 断连 > 10 秒（有持仓的交易所）
├── 价格闪崩/暴涨（1 分钟波动 > 5%）
└── 裸敞口超限（系统状态异常）

止损级（P1，快速执行）：
├── 未实现亏损 ≥ 止损金额
├── 价差反向扩大 ≥ 止损阈值
└── 保证金率接近强平线

止盈级（P2，标准执行）：
├── 动态止盈触发（价差收敛到盈利目标）
├── Trailing Stop 触发（从最优点回撤超过阈值）
└── 固定利润目标达到

定时级（P3，定时检查）：
├── 最大持仓时间到期
├── 资金费率持续不利
└── 临近资金费率结算（可选：等结算后再平）
```

### 8.3 止盈策略

#### 8.3.1 动态止盈阈值

```python
def calculate_take_profit(
    entry_spread_pct,       # 建仓时净价差率
    fee_long_maker,         # 做多所 Maker 费率
    fee_long_taker,         # 做多所 Taker 费率
    fee_short_maker,        # 做空所 Maker 费率
    fee_short_taker,        # 做空所 Taker 费率
    min_profit_pct=0.0003   # 最小净利润率（0.03%）
):
    """
    止盈阈值 = 建仓时价差 - 总成本 - 最小利润

    注意：
    - 开仓费用已经产生，不影响止盈判断
    - 止盈只需考虑平仓费用 + 最小利润
    - 平仓可能用 Taker（市价单），所以用 Taker 费率计算
    """
    # 平仓双边手续费（Taker）
    close_fees = fee_long_taker + fee_short_taker
    # 需要价差收敛的最小幅度
    required_convergence = close_fees + min_profit_pct
    # 止盈目标价差
    target_spread = entry_spread_pct - required_convergence
    return max(target_spread, -0.001)  # 允许微负值（靠资金费率补）
```

**示例**（Binance Maker 0.02% / Taker 0.05%，OKX Maker 0.02% / Taker 0.05%）：
- 建仓净价差 0.10%
- 平仓手续费 0.05% + 0.05% = 0.10%
- 最小利润 0.03%
- 需要收敛 0.13%
- 止盈目标价差 = 0.10% - 0.13% = -0.03%
- 即价差需要收敛到 -0.03%（反转）时才止盈

这说明如果净价差只有 0.10%，扣除费用后利润很薄。**这正是为什么开仓门槛要严格**。

#### 8.3.2 Trailing Stop（移动止盈）

**核心修正**：套利中我们希望价差**收敛**（从大变小），所以最优价差是**最小值**，不是最大值。

```python
class TrailingStop:
    def __init__(self, entry_spread, callback_pct=0.0005, activation_profit_pct=0.0003):
        """
        entry_spread:        建仓时价差率
        callback_pct:        回撤阈值（价差从最优点反弹多少触发平仓）
        activation_profit_pct: 激活条件（至少有多少利润才启动 trailing）
        """
        self.entry_spread = entry_spread
        self.callback_pct = callback_pct
        self.activation_profit_pct = activation_profit_pct
        self.best_spread = entry_spread  # 初始为建仓价差
        self.activated = False

    def update(self, current_spread):
        """
        每次价差更新时调用
        在套利中，价差越小对我们越有利（收敛=盈利）
        所以 best_spread 追踪的是最小价差
        """
        profit = self.entry_spread - current_spread
        
        # 检查是否达到激活条件
        if profit >= self.activation_profit_pct:
            self.activated = True

        # 只在激活后才更新最优价差
        if self.activated and current_spread < self.best_spread:
            self.best_spread = current_spread

    def should_close(self, current_spread) -> bool:
        """
        检查是否触发 Trailing Stop
        当价差从最优点（最小值）反弹超过 callback_pct 时触发
        """
        if not self.activated:
            return False
        rebound = current_spread - self.best_spread
        return rebound >= self.callback_pct

    def current_profit(self, current_spread) -> float:
        """当前理论利润（不含手续费）"""
        return self.entry_spread - current_spread
```

**示例**：
- 建仓价差 0.30%，激活利润 0.03%，回撤阈值 0.05%
- 价差收敛至 0.20% → 利润 0.10%，激活 Trailing，best = 0.20%
- 价差继续收敛至 0.12% → best 更新为 0.12%
- 价差反弹至 0.15% → 反弹 0.03%，未触发
- 价差反弹至 0.17% → 反弹 0.05%，触发平仓，锁定利润 0.30% - 0.17% = 0.13%

#### 8.3.3 固定利润止盈

```python
def check_fixed_profit(unrealized_pnl_usdt, take_profit_usdt):
    """
    unrealized_pnl_usdt: 当前未实现盈亏（USDT）
    take_profit_usdt: 止盈金额（默认 $5）
    
    计算方式：
    PnL = (current_long_price - entry_long_price) × size     # 多头盈亏
        + (entry_short_price - current_short_price) × size    # 空头盈亏
        - open_fees     # 开仓手续费（已产生）
        - est_close_fees # 预估平仓手续费
    """
    return unrealized_pnl_usdt >= take_profit_usdt
```

### 8.4 止损策略

#### 8.4.1 价差反向扩大止损

```python
def check_spread_stop_loss(current_spread, entry_spread, max_adverse_spread=0.005):
    """
    价差反向扩大（变大）意味着亏损增加
    
    current_spread: 当前价差率
    entry_spread:   建仓价差率
    max_adverse_spread: 最大允许反向扩大幅度（默认 0.5%）
    """
    adverse_move = current_spread - entry_spread
    return adverse_move >= max_adverse_spread
```

#### 8.4.2 固定金额止损

```python
def check_fixed_stop_loss(unrealized_pnl_usdt, stop_loss_usdt):
    """
    止损金额根据仓位大小动态设置，而非固定值
    
    建议：stop_loss_usdt = position_usdt × stop_loss_pct
    默认 stop_loss_pct = 1%（仓位 $500 → 止损 $5）
    """
    return unrealized_pnl_usdt <= -stop_loss_usdt
```

#### 8.4.3 最大持仓时间

```python
def check_max_hold_time(
    hold_seconds,
    max_hold_seconds=7200,           # 默认 2 小时
    next_funding_time=None,          # 下次资金费率结算时间
    funding_benefit_per_settle=0.0   # 每次结算的预期收益
):
    """
    最大持仓时间检查，但如果即将获取有利的资金费率，可以适当延长
    """
    if hold_seconds < max_hold_seconds:
        return False

    # 如果距离下次结算 < 30 分钟，且费率对我们有利，等一等
    if next_funding_time is not None:
        time_to_settle = next_funding_time - time.time()
        if 0 < time_to_settle < 1800 and funding_benefit_per_settle > 0:
            return False  # 延迟平仓，等拿到费率

    return True
```

#### 8.4.4 资金费率持续不利

```python
def check_funding_rate_adverse(
    funding_benefit_history: list,  # 最近 N 次费率差记录
    adverse_threshold=-0.0001,     # 不利阈值（-0.01%）
    consecutive_adverse=2           # 连续不利次数
):
    """
    资金费率反转检查
    
    不立即触发，需要连续 N 个周期不利才平仓（避免单次波动误杀）
    每 8 小时结算一次，所以 consecutive_adverse=2 意味着连续 16 小时不利
    """
    if len(funding_benefit_history) < consecutive_adverse:
        return False
    recent = funding_benefit_history[-consecutive_adverse:]
    return all(fb < adverse_threshold for fb in recent)
```

#### 8.4.5 保证金预警

```python
def check_margin_warning(
    margin_ratio_long,      # 做多所保证金率
    margin_ratio_short,     # 做空所保证金率
    warning_threshold=0.3   # 保证金率低于 30% 告警
):
    """
    保证金率过低时提前平仓，避免被交易所强制平仓
    强平会产生额外费用且可能部分强平导致对冲失衡
    """
    return min(margin_ratio_long, margin_ratio_short) < warning_threshold
```

### 8.5 平仓执行

```
1. 检测到平仓条件
2. 记录触发原因、当前价差、预估盈亏
3. 下单前二次确认（避免数据延迟导致误触发）：
   - P0 级：不做二次确认，立即执行
   - P1 级：快速确认（重新获取一次价格）
   - P2/P3 级：标准确认（200ms 后重新检查）
4. 两腿同时下市价平仓单
5. 等待成交（超时 3 秒）
6. 结果处理：
   ├── 两腿成交 → 记录盈亏，更新风控状态
   ├── 一腿未成交 → 进入平仓单边成交处理（见 7.5）
   └── 两腿都未成交 → 重试，若仍失败则告警
7. 更新全局风控状态
8. 发送通知（盈亏详情、触发原因）
```

---

## 9. 全局风控体系

### 9.1 多层风控架构

```
┌─────────────────────────────────────────┐
│          Layer 4: 总资金回撤保护          │  触发 → 停止所有交易，手动恢复
├─────────────────────────────────────────┤
│          Layer 3: 周级风控               │  触发 → 停止交易至本周结束
├─────────────────────────────────────────┤
│          Layer 2: 日级风控               │  触发 → 停止交易至次日
├─────────────────────────────────────────┤
│          Layer 1: 单笔/连续风控          │  触发 → 暂停 N 分钟
├─────────────────────────────────────────┤
│          Layer 0: 实时风控（每笔交易）     │  拒绝单笔不合规操作
└─────────────────────────────────────────┘
```

### 9.2 风控参数（基于总资金百分比）

| 层级 | 参数 | 默认值 | 说明 |
|------|------|--------|------|
| L0 | 单笔最大亏损 | 总资金 × 0.5% | 单笔交易亏损上限 |
| L0 | 单笔最大仓位 | 总资金 × 10% | 单笔最大名义仓位 |
| L1 | 连续亏损暂停 | 3 次 → 暂停 30 分钟 | 短期冷静期 |
| L2 | 日亏损上限 | 总资金 × 1% | 单日最大亏损 |
| L2 | 日交易次数上限 | 20 次 | 防止高频空转 |
| L3 | 周亏损上限 | 总资金 × 3% | 单周最大亏损 |
| L4 | 总回撤上限 | 总资金 × 5% | 从最高点回撤超过此值停机 |

### 9.3 完整风控实现

```python
from enum import Enum
from dataclasses import dataclass, field
from datetime import datetime, timedelta
import time


class HaltLevel(Enum):
    NONE = 0
    PAUSE = 1      # 短暂暂停（分钟级）
    DAILY = 2      # 日级停止
    WEEKLY = 3     # 周级停止
    TOTAL = 4      # 完全停止，需手动恢复


@dataclass
class RiskState:
    # 资金
    initial_capital: float = 0.0
    peak_capital: float = 0.0      # 历史最高资金（计算回撤用）
    current_capital: float = 0.0

    # 日级
    daily_pnl: float = 0.0
    daily_trade_count: int = 0
    daily_reset_date: str = ""

    # 周级
    weekly_pnl: float = 0.0
    weekly_reset_date: str = ""

    # 连续
    consecutive_losses: int = 0

    # 暂停状态
    halt_level: HaltLevel = HaltLevel.NONE
    halt_reason: str = ""
    halt_until: float = 0.0        # 暂停到什么时候（timestamp）

    # 单笔记录
    trade_history: list = field(default_factory=list)


class GlobalRiskController:
    def __init__(self, config: dict):
        """
        config = {
            'initial_capital': 10000,
            'single_loss_pct': 0.005,      # 0.5%
            'single_position_pct': 0.10,   # 10%
            'consecutive_loss_pause': 3,   # 连续亏损 3 次暂停
            'pause_duration_minutes': 30,
            'daily_loss_pct': 0.01,        # 1%
            'daily_max_trades': 20,
            'weekly_loss_pct': 0.03,       # 3%
            'total_drawdown_pct': 0.05,    # 5%
        }
        """
        self.config = config
        self.state = RiskState(
            initial_capital=config['initial_capital'],
            peak_capital=config['initial_capital'],
            current_capital=config['initial_capital']
        )

    # ── 交易前检查 ──

    def can_trade(self) -> tuple[bool, str]:
        """检查是否允许新开交易"""
        self._check_reset()

        if self.state.halt_level != HaltLevel.NONE:
            if self.state.halt_level == HaltLevel.PAUSE:
                if time.time() < self.state.halt_until:
                    remaining = int(self.state.halt_until - time.time())
                    return False, f"暂停中（剩余 {remaining}s）: {self.state.halt_reason}"
                else:
                    self.state.halt_level = HaltLevel.NONE
                    self.state.halt_reason = ""
            else:
                return False, f"交易停止: {self.state.halt_reason}"

        return True, "OK"

    def check_position_size(self, position_usdt: float) -> tuple[bool, str]:
        """检查单笔仓位是否合规"""
        max_position = self.state.current_capital * self.config['single_position_pct']
        if position_usdt > max_position:
            return False, f"仓位 ${position_usdt:.0f} 超过上限 ${max_position:.0f}"
        return True, "OK"

    # ── 交易后更新 ──

    def update_after_trade(self, pnl: float, trade_detail: dict = None):
        """每笔交易完成后调用"""
        # 更新资金
        self.state.current_capital += pnl
        if self.state.current_capital > self.state.peak_capital:
            self.state.peak_capital = self.state.current_capital

        # 更新日级
        self.state.daily_pnl += pnl
        self.state.daily_trade_count += 1

        # 更新周级
        self.state.weekly_pnl += pnl

        # 更新连续亏损
        if pnl < 0:
            self.state.consecutive_losses += 1
        else:
            self.state.consecutive_losses = 0

        # 记录
        if trade_detail:
            self.state.trade_history.append(trade_detail)

        # 检查风控
        self._check_all_limits()

    # ── 内部检查 ──

    def _check_all_limits(self):
        """检查所有风控限制，按严重程度从高到低"""
        capital = self.state.current_capital

        # L4: 总回撤
        if self.state.peak_capital > 0:
            drawdown = (self.state.peak_capital - capital) / self.state.peak_capital
            if drawdown >= self.config['total_drawdown_pct']:
                self._halt(HaltLevel.TOTAL,
                           f"总回撤 {drawdown:.2%}（上限 {self.config['total_drawdown_pct']:.2%}）")
                return

        # L3: 周亏损
        weekly_limit = self.state.initial_capital * self.config['weekly_loss_pct']
        if self.state.weekly_pnl <= -weekly_limit:
            self._halt(HaltLevel.WEEKLY,
                       f"周亏损 ${abs(self.state.weekly_pnl):.2f}（上限 ${weekly_limit:.2f}）")
            return

        # L2: 日亏损
        daily_limit = self.state.initial_capital * self.config['daily_loss_pct']
        if self.state.daily_pnl <= -daily_limit:
            self._halt(HaltLevel.DAILY,
                       f"日亏损 ${abs(self.state.daily_pnl):.2f}（上限 ${daily_limit:.2f}）")
            return

        # L2: 日交易次数
        if self.state.daily_trade_count >= self.config['daily_max_trades']:
            self._halt(HaltLevel.DAILY,
                       f"日交易 {self.state.daily_trade_count} 次（上限 {self.config['daily_max_trades']}）")
            return

        # L1: 连续亏损
        if self.state.consecutive_losses >= self.config['consecutive_loss_pause']:
            pause_seconds = self.config['pause_duration_minutes'] * 60
            self._halt(HaltLevel.PAUSE,
                       f"连续亏损 {self.state.consecutive_losses} 次",
                       pause_seconds=pause_seconds)

    def _halt(self, level: HaltLevel, reason: str, pause_seconds: float = 0):
        """设置停机状态"""
        if level.value > self.state.halt_level.value:
            self.state.halt_level = level
            self.state.halt_reason = reason
            if pause_seconds > 0:
                self.state.halt_until = time.time() + pause_seconds
            self._send_alert(level, reason)

    def _check_reset(self):
        """检查是否需要重置日/周计数"""
        today = datetime.utcnow().strftime('%Y-%m-%d')
        if self.state.daily_reset_date != today:
            self.state.daily_pnl = 0.0
            self.state.daily_trade_count = 0
            self.state.daily_reset_date = today
            # 日级停机自动重置
            if self.state.halt_level == HaltLevel.DAILY:
                self.state.halt_level = HaltLevel.NONE
                self.state.halt_reason = ""

        # 周一重置
        weekday = datetime.utcnow().weekday()
        week_id = datetime.utcnow().strftime('%Y-W%W')
        if self.state.weekly_reset_date != week_id and weekday == 0:
            self.state.weekly_pnl = 0.0
            self.state.weekly_reset_date = week_id
            if self.state.halt_level == HaltLevel.WEEKLY:
                self.state.halt_level = HaltLevel.NONE
                self.state.halt_reason = ""

    def manual_resume(self):
        """手动恢复交易（仅用于 TOTAL 级别停机）"""
        self.state.halt_level = HaltLevel.NONE
        self.state.halt_reason = ""
        self.state.halt_until = 0

    def _send_alert(self, level: HaltLevel, reason: str):
        """发送告警通知"""
        # 实现：推送到 Telegram/微信/邮件等
        pass
```

### 9.4 风控临界场景

| 场景 | 处理 |
|------|------|
| 日亏损 $95（上限 $100），正在持仓 | 持仓继续监控，但不新开仓 |
| 连续亏损 2 次，第 3 次平仓亏 $0.5 | 触发暂停 30 分钟，已有持仓继续监控 |
| 总回撤触发，但有未平仓位 | 不新开仓，已有持仓按正常止盈止损管理 |
| 日级停机后次日恢复，但周亏损也触发 | 周级优先，继续停机 |
| 暂停期间到期自动恢复，但日亏损也触发 | 日级优先，不恢复 |

---

## 10. 极端行情与异常处理

### 10.1 极端行情应急策略

```python
class MarketCircuitBreaker:
    """市场熔断器"""

    def __init__(self, config: dict):
        """
        config = {
            'flash_crash_pct': 0.05,       # 1 分钟波动 > 5%
            'liquidity_min_usdt': 1000,    # 最小深度 $1,000
            'funding_rate_max': 0.001,     # 最大费率 0.1%
            'spread_anomaly_zscore': 5.0,  # 价差异常 Z-Score
        }
        """
        self.config = config

    def check(self, market_data: dict) -> tuple[bool, str]:
        """
        返回 (is_normal, reason)
        is_normal = True: 市场正常
        is_normal = False: 触发熔断
        """
        # 1. 闪崩/暴涨检测
        if abs(market_data['price_change_1m_pct']) > self.config['flash_crash_pct']:
            return False, f"价格 1 分钟波动 {market_data['price_change_1m_pct']:.2%}"

        # 2. 流动性枯竭
        min_depth = min(market_data['bid_depth_usdt'], market_data['ask_depth_usdt'])
        if min_depth < self.config['liquidity_min_usdt']:
            return False, f"订单簿深度不足: ${min_depth:.0f}"

        # 3. 资金费率异常
        max_rate = max(abs(market_data['funding_rate_a']), abs(market_data['funding_rate_b']))
        if max_rate > self.config['funding_rate_max']:
            return False, f"资金费率异常: {max_rate:.4%}"

        # 4. 价差异常（可能交易所数据错误）
        if market_data.get('spread_zscore', 0) > self.config['spread_anomaly_zscore']:
            return False, f"价差异常 Z-Score: {market_data['spread_zscore']:.1f}"

        return True, "正常"
```

### 10.2 交易所异常处理

| 场景 | 检测 | 有持仓时 | 无持仓时 |
|------|------|----------|----------|
| API 延迟 > 500ms | 连续 3 次请求 > 500ms | 提高监控频率，准备平仓 | 暂停该所新单 |
| API 超时 5 秒无响应 | 请求超时 | 重试 2 次，仍失败则查持仓状态 | 暂停该所 |
| WebSocket 断线 | 心跳超时 10 秒 | 立即 REST 查询持仓，评估是否紧急平仓 | 暂停该所，自动重连 |
| 交易所维护 | 官方公告或连续失败 | 评估持仓风险，可能紧急平仓 | 暂停该所 |
| 订单被拒 | API 返回错误码 | 解析错误原因，调整后重试 | 记录告警 |
| 资金不足 | 下单返回余额不足 | 检查保证金率 | 暂停该所新单 |

### 10.3 系统异常处理

```python
class SystemRecovery:
    """系统恢复机制"""

    async def on_startup(self):
        """系统启动/重启后的恢复流程"""
        # 1. 从所有交易所同步当前持仓
        positions = await self.sync_positions_from_exchanges()

        # 2. 与本地数据库对比
        local_positions = await self.get_local_positions()

        # 3. 处理差异
        for pos in positions:
            if pos not in local_positions:
                # 交易所有持仓但本地无记录 → 可能是上次崩溃时开的
                log.warning(f"发现未记录持仓: {pos}")
                # 检查是否有对冲
                if self.has_hedge(pos, positions):
                    # 有对冲 → 恢复为正常持仓，继续监控
                    await self.restore_position(pos)
                else:
                    # 无对冲 → 裸敞口，紧急平仓
                    log.critical(f"裸敞口持仓: {pos}，紧急平仓")
                    await self.emergency_close(pos)

        for pos in local_positions:
            if pos not in positions:
                # 本地有记录但交易所无持仓 → 可能已被强平
                log.warning(f"持仓已消失（可能被强平）: {pos}")
                await self.mark_position_closed(pos, reason='exchange_liquidation')

        # 4. 恢复正常运行
        log.info("系统恢复完成")
```

---

## 11. 持仓状态机

每笔交易有明确的状态流转：

```
IDLE → SIGNAL_DETECTED → SIGNAL_CONFIRMED → OPENING
  ↑                                            ↓
  │                              ┌─── LEG1_FILLED ───┐
  │                              ↓                    ↓
  │                        LEG2_FILLED          LEG2_FAILED
  │                              ↓                    ↓
  │                         POSITION_OPEN      EMERGENCY_CLOSE
  │                              ↓                    ↓
  │                    ┌────────┼────────┐          CLOSED
  │                    ↓        ↓        ↓            ↑
  │              TAKE_PROFIT  STOP_LOSS  TIMEOUT      │
  │                    ↓        ↓        ↓            │
  │                  CLOSING ───────────────────── CLOSED
  │                                                   │
  └───────────────────────────────────────────────────┘
```

每次状态变更必须记录：
- 时间戳
- 触发原因
- 相关数据快照（价差、评分、盈亏等）
- 交易所订单 ID

---

## 12. 参数配置方案

### 12.1 保守型（建议初期使用）

| 参数 | 值 | 说明 |
|------|-----|------|
| 杠杆 | 3x | 低杠杆 |
| 最低评分 | 85 | 严格筛选 |
| Z-Score 门槛 | 2.0 | 只要显著偏离 |
| 信号确认窗口 | 5 秒 | 更长确认 |
| 确认比例 | 80% | 更严格 |
| 最大并发持仓 | 2 | 控制暴露 |
| 单笔仓位 | 总资金 × 5% | 小仓位 |
| Trailing 回撤 | 0.03% | 紧止盈 |
| 止损（价差扩大） | 0.3% | 紧止损 |
| 止损（固定金额） | 仓位 × 0.5% | 小亏损 |
| 最大持仓时间 | 1 小时 | 快进快出 |
| Leg Timeout | 2 秒 | 快速处理 |
| 日亏损上限 | 总资金 × 0.5% | 严格 |
| 总回撤上限 | 3% | 严格 |

### 12.2 均衡型（默认配置）

| 参数 | 值 | 说明 |
|------|-----|------|
| 杠杆 | 5x | 标准 |
| 最低评分 | 75 | 平衡 |
| Z-Score 门槛 | 1.5 | 标准 |
| 信号确认窗口 | 3 秒 | 标准 |
| 确认比例 | 70% | 标准 |
| 最大并发持仓 | 3 | 标准 |
| 单笔仓位 | 总资金 × 10% | 标准 |
| Trailing 回撤 | 0.05% | 标准 |
| 止损（价差扩大） | 0.5% | 标准 |
| 止损（固定金额） | 仓位 × 1% | 标准 |
| 最大持仓时间 | 2 小时 | 标准 |
| Leg Timeout | 3 秒 | 标准 |
| 日亏损上限 | 总资金 × 1% | 标准 |
| 总回撤上限 | 5% | 标准 |

### 12.3 激进型（有经验后使用）

| 参数 | 值 | 说明 |
|------|-----|------|
| 杠杆 | 10x | 高杠杆 |
| 最低评分 | 65 | 宽松 |
| Z-Score 门槛 | 1.2 | 更多机会 |
| 信号确认窗口 | 2 秒 | 快速 |
| 确认比例 | 60% | 更激进 |
| 最大并发持仓 | 5 | 更多 |
| 单笔仓位 | 总资金 × 15% | 大仓位 |
| Trailing 回撤 | 0.08% | 宽止盈 |
| 止损（价差扩大） | 0.8% | 宽止损 |
| 止损（固定金额） | 仓位 × 1.5% | 较大亏损容忍 |
| 最大持仓时间 | 4 小时 | 更长 |
| Leg Timeout | 5 秒 | 较长 |
| 日亏损上限 | 总资金 × 2% | 宽松 |
| 总回撤上限 | 8% | 宽松 |

---

## 13. 收益与风险预估

### 13.1 理论收益模型

```
单笔净利润 = 净价差收敛量 × 仓位 - 平仓手续费

假设（均衡型配置，$10,000 本金）：
- 平均净价差收敛：0.05%
- 平均仓位：$1,000（10x 杠杆需 $100 保证金）
- 平均每笔利润：$1,000 × 0.05% - $1 手续费 ≈ -$0.50

问题：扣除手续费后利润极薄。
```

### 13.2 现实考量

这是跨所套利最核心的挑战：**手续费占比过高**。

| 项目 | 金额 |
|------|------|
| 毛利（0.1% 价差 × $1,000） | $1.00 |
| 开仓手续费（Taker 0.05% × 2） | -$1.00 |
| 平仓手续费（Taker 0.05% × 2） | -$1.00 |
| **净利润** | **-$1.00** |

**要实现正收益，必须满足以下条件之一**：

1. **价差足够大**（> 0.2% 净价差，即毛价差 > 0.4%）
2. **使用 Maker 挂单降费**（如果牺牲速度换低费率，则用 Post-Only 方式）
3. **获取 VIP 费率**（Binance VIP3+ Maker 0%，Taker 0.023%）
4. **靠资金费率差赚持仓收益**（价差不收敛也能靠费率差盈利）

### 13.3 建议策略定位

基于以上分析，建议 Longo 的策略定位为：

**价差套利 + 费率套利 混合策略**

- 不纯粹追求价差收敛的快速交易
- 更重视建仓时的价差方向 + 资金费率差
- 允许较长持仓时间（数小时甚至跨结算周期）
- 靠每 8 小时的资金费率结算积累利润
- 价差收敛是加分项，费率差是基础收益

这样就不需要价差频繁大幅波动，降低了对交易频率的依赖。

---

## 14. 附录

### 14.1 术语表

| 术语 | 说明 |
|------|------|
| **Leg** | 套利交易的一条腿（一侧交易所的订单） |
| **Delta** | 净方向性敞口，Delta = 0 表示完全对冲 |
| **裸敞口** | 未对冲的净头寸 |
| **Z-Score** | 标准分数，表示数据偏离均值的标准差倍数 |
| **半衰期** | 价差回归均值所需的时间的一半 |
| **OU 过程** | Ornstein-Uhlenbeck 过程，均值回归随机过程模型 |
| **Maker** | 挂单方，手续费较低 |
| **Taker** | 吃单方，手续费较高 |
| **IOC** | Immediate or Cancel，立即成交或取消 |
| **Post-Only** | 仅挂单，确保以 Maker 身份成交 |
| **资金费率** | 永续合约每 8 小时结算的费用，多空之间的资金转移 |
| **逐仓** | Isolated Margin，每个仓位独立保证金 |
| **Trailing Stop** | 移动止盈/止损，跟随有利方向移动 |
| **熔断** | Circuit Breaker，极端情况下自动停止交易 |

### 14.2 关键公式

**净价差率**：
```
Net_Spread = (Ask_High - Bid_Low) / Bid_Low - Fee_Long - Fee_Short
```

**Z-Score**：
```
Z = (Current_Spread - Mean_Spread) / Std_Spread
```

**半衰期**（OU 过程）：
```
Half_Life = -ln(2) / β
其中 β 为 ΔSpread = α + β × Spread_{t-1} 的回归系数
```

**未实现盈亏**：
```
PnL = (Current_Long_Price - Entry_Long_Price) × Size
    + (Entry_Short_Price - Current_Short_Price) × Size
    - Open_Fees（已产生）
    - Est_Close_Fees（预估）
```

**保证金占用**：
```
Margin = Position_USDT / Leverage
```

**资金费率收益**：
```
Funding_Income = Position_Size × (Rate_Short_Ex - Rate_Long_Ex) × (Hold_Hours / 8)
```

**动态止盈**：
```
TP_Spread = Entry_Spread - Close_Fee_Taker_Both_Sides - Min_Profit
```

**Trailing Stop 触发**（修正后）：
```
# 价差收敛是盈利方向，best_spread 是最小值
Trigger: Current_Spread - Best_Spread >= Callback_Pct
```

---

**文档结束**
